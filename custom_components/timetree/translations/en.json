{
  "config": {
    "step": {
      "user": {
        "title": "TimeTree Credentials",
        "data": {
          "email": "Email",
          "password": "Password"
        }
      },
      "calendar": {
        "title": "Select Calendar",
        "data": {
          "calendar_id": "TimeTree Calendar",
          "scan_interval": "Poll interval (minutes)"
        }
      }
    },
    "error": {
      "invalid_auth": "Login failed. Check email/password.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "no_calendars_found": "No calendars found in this TimeTree account.",
      "already_configured": "This calendar is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "TimeTree Options",
        "description": "TimeTree does not report label names for this account via the API - enter them here yourself, matching what you see in the TimeTree app's own label/colour settings. Colours (compare against the TimeTree app to know which field is which):\n\n{label_colors}",
        "data": {
          "scan_interval": "Poll interval (minutes)"
        }
      }
    }
  }
}
